[package]
name = "hello-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde_json = "1.0"
tower-http = { version = "0.6.6", features = ["cors"] }